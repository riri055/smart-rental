# Base URL of the Smart Rental backend API.
# The Vite dev server reads VITE_* variables from a local `.env` file.
# If unset, the app defaults to http://127.0.0.1:8000.
VITE_API_BASE_URL=http://127.0.0.1:8000
